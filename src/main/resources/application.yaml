spring:
  application:
    name: platform
  datasource:
    url: jdbc:postgresql://testing.cilssceec3gr.us-east-1.rds.amazonaws.com:5432/postgres
    username: postgres
    password: a1203zxcv!
    driver-class-name: org.postgresql.Driver
  jpa:
#    hibernate:
#      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
